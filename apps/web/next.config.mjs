/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  turbopack: {
    root: '/tmp/cc-agent/67823963/project/apps/web',
  },
}

export default nextConfig
